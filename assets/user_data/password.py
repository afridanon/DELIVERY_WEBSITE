import re
from username import username
def password():
    global password
    password =re.url("https://github.com/scmylsc/passwords/blob/main/passwords.txt") 
class send_msg:
    def __init__(username,password):
        global username1
        global password2
        password1 = int(password)
        username2 = int(username)
    def msg_wa(password1,username1):
        print(int(username1) + int(password))
 class collect_num():
    def __init__(no,name):
        this.no = number
        this.name = user_name
     def send(number,user_name):
        
        
if __name__ == "__main__":
    noo = int(input("Enter Your Number"))
    if(length(noo) < 10):
        return 1
    else if(length(noo) > 10):
        return 2
    else :
        return 0
    name = str(("Enter your Name: "))
    collect(noo,name)
